<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="Story_App_HW_PORTILLO.NAOMY">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NameActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".ProfessionActivity" />
        <activity android:name=".FinalStoryActivity" />
        <activity android:name=".FavoriteThingActivity" />
        <activity android:name=".FavoriteTool" />
        <activity android:name=".SomethingDeliciousActivity" />
        <activity android:name=".VillainActivity" />
        <activity android:name=".RewardActivity"></activity>
    </application>

</manifest>